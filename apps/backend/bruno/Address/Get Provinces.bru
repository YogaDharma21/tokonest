meta {
  name: Get Provinces
  type: http
  seq: 1
}

get {
  url: {{app_url}}/api/province
  body: multipartForm
  auth: inherit
}

headers {
  Accept: application/json
  Authorization: Bearer {{sanctum_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}

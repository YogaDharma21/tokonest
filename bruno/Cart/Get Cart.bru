meta {
  name: Get Cart
  type: http
  seq: 2
}

get {
  url: {{app_url}}/api/cart
  body: multipartForm
  auth: inherit
}

headers {
  Accept: application/json
  Authorization: Bearer {{sanctum_token}}
}
